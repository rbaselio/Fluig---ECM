function beforeTaskSave(colleagueId,nextSequenceId,userList){
	
	var numAtividade = getValue("WKNumState");
	
	/*if (numAtividade == 0 || numAtividade == 1 ){
		if (hAPI.listAttachments().size() < 1 && hAPI.getCardValue("tipo") == 'sistema') {
			throw "<br/><strong>ATENÇÃO:</strong><br/><br/>É obrigatorio incluir um print, video ou relatorio evidenciando o problema<br/><br/>.";
		}
	}*/
	
	
	
}